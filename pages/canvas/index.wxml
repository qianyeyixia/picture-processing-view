<!--pages/canvas/index.wxml-->
<view>
    <view class="header">
        <view class="upload_img" bindtap="chooseImage">上传图片</view>
    </view>
    <image wx:if="imgsrc" src="{{imgSrc}}" style="width: 150rpx;height: 150rpx;"></image>
    <button class="cu-btn" bindtap="toCropper">前往cropper页面</button>
</view>